export const STORAGE_KEYS = {
  schemaVersion: 'cutlog.schemaVersion',
  profile: 'cutlog.profile',
  goal: 'cutlog.goal',
  settings: 'cutlog.settings',
  foods: 'cutlog.foods',
  savedFoods: 'cutlog.savedFoods',
  savedMeals: 'cutlog.savedMeals',
  workouts: 'cutlog.workouts',
  activities: 'cutlog.activities',
  weights: 'cutlog.weights',
  activeWorkout: 'cutlog.activeWorkout',
  aiDebug: 'cutlog.aiDebug',
} as const;
